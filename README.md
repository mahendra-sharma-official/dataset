# Simple dataset header only class

# Supports basic features
1. Loading from csv, from matrices
2. Supports normalizations (minmax, standard) and denormalizations
3. Train test split (from ratio) 
4. Shuffling of data

# Note: Use c++17 or more